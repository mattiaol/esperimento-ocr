streamlit==1.47.0

# FIX FONDAMENTALE: Rimuoviamo il conflitto tra opencv-python e headless.
# Usiamo solo headless e forziamo una versione moderna nota per essere stabile.
opencv-python-headless==4.8.0.76

# Librerie OCR (lasciamo le versioni vecchie per evitare nuovi problemi di Torch/TF)
craft-text-detector==0.4.2
easyocr==1.4.1

# Dipendenze pesanti (bloccate alle versioni originali ma compatibili con 3.10)
torch==1.11.0
torchvision==0.12.0
numpy<2.0.0
scipy==1.8.0

# Altre dipendenze
Pillow
imutils
shapely
networkx
