streamlit==1.47.0
numpy<2.0.0

# 2. **FIX FONDAMENTALE**: Forziamo OpenCV a una versione moderna (e headless per sicurezza)
# Una versione sopra il 4.5.4.61 (che era il limite di craft) è necessaria.
# Usiamo l'ultima 4.x stabile per aggirare il blocco di craft.
opencv-python-headless==4.8.0.76

# 3. Le librerie che usano OpenCV (verrà usata la versione che abbiamo appena installato)
craft-text-detector
easyocr

# 4. Dipendenze accessorie
Pillow
pillow-heif
scipy
imutils
shapely
networkx
