streamlit==1.47.0
# Teniamo numpy < 2 per sicurezza (fondamentale)
numpy<2.0.0

# Librerie principali
# NON mettiamo opencv qui: craft-text-detector installerà automaticamente
# la versione standard (opencv-python). Visto che abbiamo il file packages.txt,
# ora funzionerà anche su Cloud!
craft-text-detector
easyocr

# Dipendenze grafiche e di calcolo
Pillow
pillow-heif
scipy
imutils
shapely
networkx
